#include <bits/stdc++.h>
using namespace std;


int main(){
    //将所有输入的字符串连接成一个字符串
    // string s;
    // string str;
    // while (cin >> s) {
    //     str += s;
    // }
    // cout << str << endl;
    // return 0;
    
    // 输出" "连接后的字符串
    string s;
    string str;
    while (cin >> s) {
        str += s + " ";
    }
    cout << str << endl;
    return 0;
}